import typescript from '@rollup/plugin-typescript';
import resolve from 'rollup-plugin-node-resolve';
import { terser } from "rollup-plugin-terser";

export default {
  input: './src/index.ts',
  output: {
    file: './dist/lib/sa-lambda.min.js',
    format: 'umd',
    name: 'sa-lambda',
  },
  plugins: [typescript(), terser({
    format: {
      comments() {
        return false
      }
    }
  }), resolve()]
};